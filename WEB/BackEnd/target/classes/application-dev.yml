spring:
  web:
    resources:
      static-locations: classpath:/static/,classpath:/public/,file:./static/

  application:
    name: Backend

  devtools:
    restart:
      enabled: true

  mail:
    default-encoding: UTF-8
    host: smtp.163.com
    username: jsk476@163.com
    password: RGbiENkGuAA57Vm3
    port: 465
    properties:
      mail:
        smtp:
          ssl:
            enable: true
          auth: true
          starttls:
            enable: true
            required: true

  datasource:
    url: jdbc:mysql://localhost:3306/ZyroFrp
    username: root
    password: Shajie@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true

    open-in-view: false

server:
  port: 8085

logging:
  level:
    root: INFO
    cn.zyroo: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%X{traceId}] %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%X{traceId}] %logger{36} - %msg%n"
  file:
    name: logs/ZyroFrp-dev.log
    max-size: 100MB
    max-history: 30
